<h1><%= @animal.name %></h1>

<h2><p>Food: <%= @animal.food %></p></h2>
<%= image_tag @animal.image_url %>
<hr>
<%= link_to "[Edit]", edit_animal_path(@animal.id) %>
<%= link_to "[Delete]", animal_path(@animal.id) ,method: :delete,data: { confirm: "Are you sure? "}%>
